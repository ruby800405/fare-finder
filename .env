# Supabase — this project's own instance (flight_fare_notifier).
# Only VITE_-prefixed variables reach the browser bundle; both are inlined at build time.
# The publishable key is browser-safe and RLS-gated (it replaces the former anon key).
VITE_SUPABASE_URL=https://pzridshqerbdxtktajtc.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_pEL0S-7FZO19gddEq8l-aQ_Y0aL14ko
